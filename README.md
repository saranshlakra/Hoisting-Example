# Hoisting-Example


-------Hoisting Example-------
Hoisting means every declared variable and function will be shifted to the top of the scope.
For example: you can call the function before execution .
